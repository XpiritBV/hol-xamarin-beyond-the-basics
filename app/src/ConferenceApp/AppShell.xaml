<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:d="http://xamarin.com/schemas/2014/forms/design"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:sessions="clr-namespace:ConferenceApp.Content.Sessions"
       xmlns:speakers="clr-namespace:ConferenceApp.Content.Speakers"
       xmlns:local="clr-namespace:ConferenceApp.Views"
       xmlns:styles="clr-namespace:ConferenceApp.Styles"
       xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
       mc:Ignorable="d" Title="ConferenceApp"
       x:Class="ConferenceApp.AppShell">

    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.TitleColor" Value="{StaticResource TextNavigationColor}"/>
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource NavigationBackground}"/>
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource NavigationBackground}" />
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Your Pages -->
    <TabBar Style="{DynamicResource CurrentTheme}">
        <Tab Title="Sessions" >
            <Tab.Icon>
                <FontImageSource FontFamily="{StaticResource FontAwesome}"
                                 Glyph="{x:Static styles:IconFont.Calendar}"
                                 Size="18" />
            </Tab.Icon>
            <ShellContent ios:NavigationPage.IsNavigationBarTranslucent="True" ios:NavigationPage.PrefersLargeTitles="True" ContentTemplate="{DataTemplate sessions:SessionsPage}" />
        </Tab>
        <Tab Title="Speakers">
            <Tab.Icon>
                <FontImageSource FontFamily="{StaticResource FontAwesome}"
                                 Glyph="{x:Static styles:IconFont.UserFriends}"
                                 Size="18" />
            </Tab.Icon>
            <ShellContent ios:NavigationPage.IsNavigationBarTranslucent="True" ios:NavigationPage.PrefersLargeTitles="True" ContentTemplate="{DataTemplate speakers:SpeakersPage}" />
        </Tab>
        <Tab Title="About">
            <Tab.Icon>
                <FontImageSource FontFamily="{StaticResource FontAwesome}"
                                 Glyph="{x:Static styles:IconFont.InfoCircle}"
                                 Size="18" />
            </Tab.Icon>
            <ShellContent ios:NavigationPage.IsNavigationBarTranslucent="True" ios:NavigationPage.PrefersLargeTitles="True" ContentTemplate="{DataTemplate local:AboutPage}" />
        </Tab>
    </TabBar>

    <!-- Optional Templates 
    // These may be provided inline as below or as separate classes.

    // This header appears at the top of the Flyout.
    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid>ContentHere</Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    // ItemTemplate is for ShellItems as displayed in a Flyout
    <Shell.ItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Title, Icon
            </ContentView>
        </DataTemplate>
    </Shell.ItemTemplate>

    // MenuItemTemplate is for MenuItems as displayed in a Flyout
    <Shell.MenuItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Text, Icon
            </ContentView>
        </DataTemplate>
    </Shell.MenuItemTemplate>

    -->
</Shell>